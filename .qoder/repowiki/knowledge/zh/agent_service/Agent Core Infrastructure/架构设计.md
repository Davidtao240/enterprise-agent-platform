- **配置层**：基于 `pydantic_settings` 的 `Settings` 类统一管理环境变量，涵盖 LLM、MinIO 及 Qdrant 连接信息。
- **服务层**：`llm.py` 封装 `ChatOpenAI` 实例化逻辑，支持多模型提供商动态切换；`file_utils.py` 实现“内联数据 → MinIO/文件服务 → 内置样例”的三级数据加载降级策略。
- **监控层**：`UsageTracker` 负责聚合 LangChain 调用产生的 Token 消耗并估算成本，为上层图执行提供可观测性支持。