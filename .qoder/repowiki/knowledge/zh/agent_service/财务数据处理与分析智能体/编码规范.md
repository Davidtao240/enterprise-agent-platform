- 所有智能体类均继承自 `BaseAgent` 并显式声明 `agent_id` 和 `domain` 类属性以支持路由与识别。
- 涉及 LLM 调用的智能体均在 `try-except` 块中封装调用逻辑，并在异常时降级执行本地定义的 `_build_fallback_...` 函数。
- LLM 提示词工程统一要求模型返回纯 JSON 格式，并通过移除 markdown 代码块标记（```json）后使用 `json.loads` 进行解析。
- 智能体的 `run` 方法严格遵循异步签名 `async def run(self, state: dict[str, Any]) -> dict[str, Any]`，并通过修改并返回 `state` 对象来传递执行结果或错误信息。