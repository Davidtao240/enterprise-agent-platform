- 读写模型分离：定义 `AuditLog` 用于全量数据存储，`AuditLogEntry` 用于写入输入，`ListResponse` 用于精简 API 响应，避免暴露不必要的内部字段。
- 动态 SQL 构建：在 `ListLogs` 中根据非空查询参数动态拼接 `WHERE` 子句及占位符，实现灵活的多条件过滤。
- 统一分页处理：在 Handler 层解析并校验 `page` 和 `page_size` 参数，设置默认值及上限（100），确保查询性能可控。