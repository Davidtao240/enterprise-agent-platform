- 页面组件按功能模块划分在 `src/pages/` 目录下，并直接以 `Page.tsx` 后缀命名（如 `LoginPage.tsx`, `DashboardPage.tsx`）。
- API 请求逻辑集中定义在 `src/services/api.ts` 中，按业务领域（Auth, Workflows, Approvals 等）分组导出异步函数。
- 全局状态通过 Zustand 的 `create` API 定义，并结合 `localStorage` 实现状态的持久化加载与同步更新。