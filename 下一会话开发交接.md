# Enterprise Agentic Platform 下一会话开发交接

> 更新时间：2026-08-16
> 当前主线：M1 Durable Agent Run
> 用途：新建 Codex 会话后，先读取本文件和权威文档，再开始代码迭代。
> 注意：文档中的 Target 能力不等于代码已经实现，必须使用 CodeGraph 和测试核对。

## 1. 新会话直接发送的提示词

```text
请在 /Users/jinli/Project/enterprise-agent-platform 中继续开发。

先完整读取并遵守：
1. 根目录 AGENTS.md；
2. 根目录 下一会话开发交接.md；
3. docs/README.md；
4. docs/05_FUTURE/ENTERPRISE_AGENTIC_PLATFORM_EVOLUTION.md；
5. docs/01_PROJECT/ROADMAP.md；
6. docs/02_ARCHITECTURE/ARCHITECTURE.md；
7. docs/02_ARCHITECTURE/DOMAIN_MODEL.md；
8. docs/02_ARCHITECTURE/GRAPH_RUNTIME_SPEC.md；
9. docs/02_ARCHITECTURE/AGENT_IO_CONTRACT.md；
10. docs/02_ARCHITECTURE/ADR-002_CONTROL_PLANE_RUNTIME_AUTHORITY.md 和 ADR-003_DURABLE_AGENT_RUN.md；
11. docs/03_PLATFORM_SPEC/DATABASE_SCHEMA.md、API_DESIGN.md、API_CONTRACT.md、WORKFLOW_STATE_MACHINE.md。

使用 CodeGraph 核对当前 Workflow → Asynq Worker → Go Agent Gateway → Python Finance Graph → Agent Run Log 调用链及影响范围，然后检查 git status 和相关 diff。不得回滚或覆盖用户现有未提交改动；go-platform/internal/workflow/worker.go 当前有用户添加的学习注释，若需要修改必须保留并最小化合并。

不要开始 Procurement Graph、采购 UI、真实数据库/ERP/工单接入。当前只进入 M1 Durable Agent Run。

先给出 M1-A 第一批的具体实现计划和风险，然后完成一个可验证的最小纵向闭环：
- 建立 Thread/Run/Step/Runtime Event 的领域模型、migration 和 repository；
- 在不破坏 Finance V1 /internal/v1/agent-runs 契约的前提下，让现有 agent_graph 执行关联 Durable Run 生命周期；
- 保留 agent_run_logs 兼容查询，明确双写或聚合策略；
- 对 Run 状态转换、Tenant 隔离、重复 Event/Attempt 和 Finance 兼容增加测试；
- 本批不提前实现完整 Tool Gateway、Connector、Memory 或 Procurement。

修改 3 个及以上文件后，按 AGENTS.md 完成跨栈契约、架构、安全、死代码和扩展性自审。运行定向测试和 bash scripts/check.sh；如环境阻止某项验证，准确说明，不得声称通过。不要 commit/push，除非我明确要求。
```

## 2. 文档权威顺序

```text
L1 目标：docs/05_FUTURE/ENTERPRISE_AGENTIC_PLATFORM_EVOLUTION.md
L2 阶段：docs/01_PROJECT/ROADMAP.md
L3 实现契约：docs/02_ARCHITECTURE + docs/03_PLATFORM_SPEC
L4 兼容基线：docs/04_V1_FINANCE + Finance Regression Fixture
L5 延期场景：docs/06_PROCUREMENT
```

不能只读取 Procurement 文档决定开发顺序。采购设计是带 Gate 的场景规范，不是当前 Roadmap。

## 3. 当前仓库事实

- 项目路径：`/Users/jinli/Project/enterprise-agent-platform`
- 当前分支：`agent/phase-a-shared-core`
- 记录时 HEAD：`f45ba0a`
- 工作区存在未提交内容，开始前必须重新执行 `git status --short`。
- `go-platform/internal/workflow/worker.go` 有用户学习注释，不得覆盖或删除。
- `AGENTS.md`、`docs/06_PROCUREMENT/`、本交接文件等可能仍为未跟踪文件，不得自行删除、提交或推送。
- `.codegraph/` 已存在；代码定位和影响分析先使用 CodeGraph。

分支、HEAD 和工作区状态会变化，本节只作为历史快照，不能替代现场检查。

## 4. 已完成的过渡工作

Phase A 第一批 A1–A3 已完成实现并通过当次质量闸门，待正式验收：

- `agent-service/tests/fixtures/finance_v1_contract.json`。
- Finance Contract Regression Test。
- `app/profiles/contracts.py` 和 Versioned Finance Profile。
- DataExtract、SchemaMapping、Validation、Report、ReviewSummary Shared Core。
- Finance Graph 显式绑定 Profile。
- Python/Go Shared/Domain Metadata 对齐。
- Finance V1 Graph 顺序、状态键、输出键和 Agent Run Envelope 保持兼容。

这不表示以下能力已经完成：

- Durable Thread/Run/Step。
- Python 持久化 Checkpointer。
- Interrupt/Resume/Cancel。
- Go Tool Execution Gateway。
- Connector/CredentialRef。
- 完整 Skill/Memory/Trace/Eval。
- Procurement Graph 或真实企业系统接入。

## 5. 当前目标：M1 Durable Agent Run

M1 要把当前一次性同步 Graph 调用升级为：

```text
Workflow Node
└── Durable Run
    ├── Step
    ├── Checkpoint
    ├── Interrupt
    ├── Resume / Cancel
    └── Runtime Event
```

完成 Gate：

1. Python 进程中断后可以从正确 Checkpoint 恢复。
2. Go Worker 中断或消息重复时不重复推进状态。
3. 重复 Resume/Retry 幂等。
4. 等待审批期间重启服务后仍可继续。
5. 迟到 attempt 不能覆盖新 attempt。
6. Finance V1 Regression 持续通过。

## 6. M1 实施批次

### M1-A：Go Durable Run 骨架与兼容关联（已完成并通过真实 PostgreSQL 验收）

- 核对当前 run id 的生成方和 `agent_run_logs` 写入链。
- 设计并实现 Thread、Run、Step、Runtime Event 的 additive migration。
- 实现 repository/service 状态转换和幂等约束。
- 让现有 Finance `agent_graph` 执行创建和更新 Durable Run。
- 保留 `/internal/v1/agent-runs` 与 `agent_run_logs` 查询兼容。
- 增加状态、Tenant、重复 Event/Attempt 和回归测试。

012/013/014 additive migrations、V1 Finance Gateway 兼容链、Tenant 复合外键、
Attempt/Event 幂等和真实 PostgreSQL 仓储验收已经完成。`agent_run_logs` 继续
作为兼容摘要。

### M1-B：Python Checkpointer 与 Runtime V2 Contract（已完成当前单实例范围）

- 实现 `/internal/v2/agent-runs` Start/Resume/Cancel。
- 持久化 Checkpoint 和单调 Checkpoint Version。
- Runtime Event 至少一次投递，Go 幂等消费。
- Interrupt/Resume Schema 和重复 Resume 防护。

当前实现补充：

- `AsyncSqliteSaver` + Docker `agent_runtime_data` 持久卷。
- Python Start/Resume/Cancel service-auth routes、命令幂等表和 Event Outbox。
- Go Runtime V2 Client、service-auth Event endpoint、Checkpoint/Interrupt 索引。
- 单调 Checkpoint Version、重复 Start/Resume/Cancel/Event、Tenant、迟到
  Attempt 和等待态重启测试。
- Finance V1 与 V2 均已跑通 Go→Python→Go→PostgreSQL 真实跨服务验收。

可重复验收命令（Docker 服务启动后）：

```bash
INTERNAL_SERVICE_TOKEN='m1-local-acceptance-token' LLM_API_KEY= \
docker compose up -d --build postgres redis minio qdrant go-backend agent-service

cd go-platform
TEST_DATABASE_URL='postgres://platform:platform_dev@localhost:5432/enterprise_agent_platform?sslmode=disable' \
TEST_AGENT_SERVICE_URL='http://localhost:8000' \
TEST_INTERNAL_SERVICE_TOKEN='m1-local-acceptance-token' \
go test ./internal/agent -run TestDurableRepositoryPostgresAcceptance -v

TEST_DATABASE_URL='postgres://platform:platform_dev@localhost:5432/enterprise_agent_platform?sslmode=disable' \
go test ./internal/database -run TestFreshMigrationChainPostgresAcceptance -v
```

下一批从 M1-C 开始；在 lease/heartbeat 和 Workflow 事件驱动完成前，不把
Finance Worker 从 V1 桥强行切到 V2。

### M1-C：Lease、Heartbeat 与故障实验

- Worker lease/heartbeat/attempt。（M1-C-A，已实现）
- 失联接管和迟到结果拒绝。（M1-C-A 接管 + M1-C-B 迟到结果拒绝，已实现）
- Python/Go/审批等待重启实验。（已取证，见下方 M1-C 取证记录）
- 全量 Finance Contract、Go、Python、Frontend 和 Security Gate。（已通过）

M1-C-A：`agent_runs` lease 列（owner/expires/heartbeat）、每次执行唯一
owner token、条件 UPDATE 原子获取、Python 调用期间心跳续租、收敛扫描器
基于 lease 过期接管并以同一 Run 身份重驱动（幂等恢复）。

M1-C-B（迟到结果拒绝，2026-08-17 批次）：`V1DurableRunCompletion` 携带
`LeaseOwner`；Run 非终态且 lease 持有者是其他执行者时，完成写入返回
`ErrLeaseNotHeld`（同状态幂等重放与空 owner 存量路径不受影响）。Gateway
被拒时不向 Worker 报错，而是回放 Run 当前状态，避免 Worker 触发
OnNodeFailed 回退接管者正在执行的节点。测试：
`TestDurableRunCompleteRejectsForeignLeaseOwner`、
`TestGatewayLateSuccessRejectedAfterLeaseTakeover`、
`TestGatewayLateFailureRejectedAfterLeaseTakeover`、
`TestDurableRunLateResultRejectionPostgresAcceptance`（真实 PostgreSQL）。

M1-C 已完成（含 Docker 故障实验取证与全量闸门，见下方记录）。下一批从
M2 开始。在 lease/heartbeat 和 Workflow 事件驱动完成前，不把 Finance
Worker 从 V1 桥强行切到 V2；切 V2 异步路径的前提（run.succeeded 事件
携带最终输出）列为 M2 首项。

M1-C 取证（2026-08-17，Docker 全量重建后执行
`AUTO=1 bash scripts/m1-fault-experiments.sh`，六项全部通过）：

1. 杀 Python 后恢复：Run `succeeded`（attempt=1），工作流经审批归档
   `archived`。注：无 LLM 确定性模式下 Run 毫秒级完成，重启未必命中
   执行中；命中时验证 checkpoint 恢复（此前 run 已观察到 running 中被
   杀后恢复），未命中时验证正常完成，终态断言一致。
2. 杀 Go Worker 事件幂等：runtime_events 断言 `4|4|4`
   （total=distinct_event_ids=distinct_seq），节点单终态不重复推进，
   重启后闭环归档。
3. Start 重放/失联接管：lease 置过期后收敛扫描器以同一 attempt=1
   重驱动，断言前后均仅一行 `attempt=1|count=1`，无 attempt=2。
4. 等待审批全量重启：审批任务为经典 human_review
   （runtime_interrupt=f、interrupt_id 空），restart 全部服务后
   approve 生效（`approved|decided=t`），工作流 `archived`。
5. ToolCall Reconcile：占位断言通过（tool_steps=0、tool_calls 表
   未创建），真实验收归 M2 Tool Execution Gateway。
6. 容器内 Python 回归：24 测试 OK（skipped=1 为
   `test_python_metadata_matches_go_seed`：agent-service 容器内无
   go-platform 仓库，跨栈 seed 断言仅在完整仓库的本地闸门强制执行）。
`bash scripts/check.sh` 全量闸门通过（Go/Python/前端/安全；本地
Python 24/24 无 skip）。

实验手册修复（bash 3.2 兼容与流程修正，2026-08-17 批次）：

- `docker restart <svc>` → `docker compose restart <svc>`（容器名带
  compose 项目前缀，原命令必然失败）。
- login/create_finance_demo 的 JSON body 改为 printf 预构造：macOS
  bash 3.2 对「双引号命令替换内嵌套 `\"` 转义」存在词法缺陷，会把
  一条 curl 拆成两条。
- `$var` 后紧跟多字节 UTF-8 字符改为 `${var}` 边界写法（bash 3.2
  变量名吞字节导致 set -u 误报 unbound variable）。
- `field`/`json_get` 支持 `data.0.id` 数字段数组索引（原实现对 list
  用字符串键静默返回空串，审批查询因此失败）。
- 实验 1..3 增加 `wait_pending_approval`/`approve_and_archive` 收尾：
  Finance V1 演示流程必经 human_review 审批才能 archived，原脚本直接
  等 `archived` 的断言过强。
- 实验 1 断言 A 期望修正：`checkpoint_version` 由 V2 Runtime Event
  路径维护，V1 桥执行允许为空（实验 2 的事件断言覆盖 V2 侧）。

## 7. M1 的关键设计边界

### Workflow 与 Run

- Workflow 是企业业务流程。
- Run 是 Agent 内部持久执行。
- Run `succeeded` 不代表 Workflow `archived`。
- Node Retry 保留旧 Run/Attempt，不覆盖失败事实。

### Go 与 Python

- Go 保存企业 Run Index、Workflow 关联、配置版本、Approval、Audit 和 Event 去重。
- Python Checkpointer 保存 Graph State、Cursor、局部消息和恢复点。
- 外部系统仍是其业务对象的最终事实来源。

### 兼容策略

- Finance V1 `/internal/v1/agent-runs` 请求响应保持兼容。
- `agent_run_logs` 暂不删除；它是兼容摘要，不是 Checkpoint。
- 新表和新 API additive 引入。
- Breaking Contract 必须使用新 Major Version。

## 8. 当前禁止事项

- 不开发完整 Procurement Graph 或采购 UI。
- 不接入真实数据库、ERP、工单或供应商数据。
- 不开放任意 SQL、HTTP、Shell、Filesystem、Browser、Email Tool。
- 不在 Python 保存企业管理员凭证。
- 不把 Tool Registry 当作 Tool Execution Gateway。
- 不提前建设完整 Memory 或复杂 Multi-Agent。
- 不在 Go Core 增加 Finance/Procurement 条件分支。
- 不因文档写了 Target Schema/API 就假设代码已经实现。

## 9. M1 后续顺序

```text
M1 Durable Agent Run
→ M2 Tool Execution Gateway
→ M3 Connector Runtime
→ M4 Context / Skill / Memory
→ M5 Trace / Eval / Replay
→ M6 Workbench / Authorized Router
→ Procurement Phase B 或其他业务场景
```

## 10. 验证与汇报要求

完整质量闸门：

```bash
bash scripts/check.sh
```

M1 还必须增加定向测试和故障实验，不能只依靠现有全量测试。

完成一批后汇报：

- 修改文件和数据迁移。
- Workflow/Run/Step 的实际调用链。
- V1 兼容与数据迁移策略。
- 状态机和幂等不变量。
- 定向测试、故障实验和完整质量闸门结果。
- 自审发现及修复。
- 未关闭风险和下一批建议。
- 明确是否未 commit/push。

---

最重要的一句话：

> 下一步不是继续横向增加业务 Agent，而是先让现有 Finance 主链拥有真正可持久、可恢复、可取消和可验证的 Durable Agent Run。
