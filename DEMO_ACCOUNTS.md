# 本地演示账号

本项目本地开发和演示环境的默认密码均为：

```text
password
```

| 账号 | 密码 | 说明 |
|---|---|---|
| `admin` | `password` | 平台管理员 |
| `finance_user` | `password` | 财务业务用户 |
| `finance_manager` | `password` | 财务审批/管理用户 |
| `ops_viewer` | `password` | 运维只读用户 |

本地混合开发访问地址：

```text
http://localhost:5173
```

全 Docker 演示访问地址：

```text
http://localhost:3000
```

注意：这些账号仅用于本地开发和演示，不要用于生产环境。
